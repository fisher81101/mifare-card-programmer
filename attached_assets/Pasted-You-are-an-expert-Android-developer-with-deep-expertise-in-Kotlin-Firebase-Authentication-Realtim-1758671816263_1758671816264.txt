You are an expert Android developer with deep expertise in Kotlin, Firebase (Authentication, Realtime Database, Cloud Messaging), NFC for RFID programming, and secure app development. You also have expertise in web development using JavaScript, React, and Firebase Web SDK for building admin interfaces. Your task is to generate complete, production-ready code for a modified Android client app and a new web-based admin app. The client app allows customers to program RFID cards remotely under admin control, using their phones as encoders without direct access to programming features. The web admin app enables remote control to fill out fields and program cards on the customer’s device, integrating with a pre-existing web interface for SALTO programming decoding and pushing custom data. Output the code as an Android project and a web project (folders: client_app_modified, admin_web) in a ZIP file or structured directories, including all source files, build.gradle (for Android), package.json (for web), index.html/app.js (for web), AndroidManifest.xml, and a README.md with setup instructions. Provide Firebase setup instructions and any server-side configurations, including sample integration for SALTO programming decoding if applicable (use logical defaults like JSON payloads for SALTO-compatible MIFARE data).
Project Overview • Existing Client App: An Android app (written in Kotlin) that lets customers program RFID cards purchased from me. It uses NFC to read/write data to RFID cards. Key features: ◦ UI with fields for RFID data (e.g., card ID, user name, access level, expiration date), but modified to restrict local programming access. ◦ NFC integration to write data to RFID cards (assume MIFARE Classic or similar tags, compatible with SALTO systems for hotel-like encoder functionality). ◦ Local storage for programmed card history. ◦ Built with Android Jetpack (ViewModel, LiveData, Navigation), Material Design, targeting API 24+. ◦ No current remote features or user accounts (standalone app). • Goals: ◦ Modify Client App: Add Firebase Authentication, remote data receiving, and support for the web admin to remotely fill fields and trigger card programming without customer approval. Ensure customers cannot program cards locally without admin push; the app acts as a remote encoder controlled by the admin. ◦ Create Web Admin App: A React-based web interface that allows me (the admin) to select a customer, fill out RFID programming fields (including custom SALTO-decoded data), send data to their client app, and trigger programming remotely via Firebase. Integrate with existing web app components for SALTO decoding and pushing programming to blank cards, enabling remote hotel-like encoder capabilities on any Android phone. ◦ Use Firebase for backend: Authentication for logins, Realtime Database for data syncing, Cloud Messaging for notifications. ◦ Ensure security: HTTPS, encrypted data, role-based access (admin vs. customer). ◦ No screen sharing; admin sends JSON data (potentially including decoded SALTO programming payloads) to client app, which auto-populates fields and prompts for card placement. Detailed Requirements for Modified Client App • Authentication: ◦ Integrate Firebase Authentication: Support email/password login and Google Sign-In. ◦ Each customer has a unique account tied to their device (use Firebase UID). ◦ On first launch, prompt user to create or log in to an account. • Remote Control Support: ◦ Add a “Remote Session” mode: Admin can initiate a session without customer approval, using the app solely as an encoder. ◦ App listens to Firebase Realtime Database (e.g., /remote_sessions/{customer_uid}) for incoming data. ◦ When data arrives (JSON with field values, e.g., {card_id, user_name, access_level, expiration_date}, and optional SALTO-specific programming data like sector keys or encoded blocks): ▪ Auto-populate the programming form fields (hidden from user for local input). ▪ Display a prompt: “Place RFID card to program” with a “Cancel” button. ▪ Trigger NFC write when card is detected, supporting MIFARE programming for SALTO-compatible cards. ◦ Send success/failure status back to admin via Firebase (e.g., /remote_sessions/{customer_uid}/status). ◦ Session ends automatically after programming, on timeout (e.g., 10 minutes), or if admin/customer cancels. ◦ Show session status (e.g., “Admin is programming your card”). ◦ Restrict local programming: Hide or disable UI fields for manual input; app only activates for remote pushes. • NFC Programming: ◦ Retain existing NFC functionality: Use Android NFC APIs (NfcAdapter, TechList, Ndef) to write data to RFID cards, enhanced for MIFARE Classic with SALTO decoding support (e.g., write to specific sectors/blocks). ◦ Enhance: Validate data before writing (e.g., non-empty fields, SALTO format checks), handle errors (card not detected, write failure). ◦ In remote mode: Automatically write received data (including custom SALTO payloads) when card is tapped. • Push Notifications: ◦ Integrate Firebase Cloud Messaging (FCM): Notify customer when a remote session starts or data is received. ◦ Example notification: “Admin is preparing to program your RFID card.” • UI/UX: ◦ Main screen: Minimal UI showing status; form fields are auto-populated and locked during remote sessions (no local entry to prevent unauthorized programming). ◦ Add a “Request Admin Help” button to notify admin (via FCM). ◦ During remote session: Lock any local inputs, display received data, show “Place card to program” prompt. ◦ Use Material 3 components for a modern UI. • Security & Privacy: ◦ Encrypt sensitive data (e.g., card info, SALTO keys) using Android Keystore before writing to card or Firebase. ◦ Store session logs in Firebase for auditing (e.g., /session_logs/{session_id}). ◦ Use Firebase Security Rules to restrict access (customers can only read/write their own data). • Other: ◦ Offline support: Queue programming tasks if offline, sync when online. ◦ Error handling: User-friendly messages for network issues, NFC failures, etc. ◦ Permissions: Request NFC, internet, and notification permissions. Detailed Requirements for New Web Admin App • Authentication: ◦ Firebase Auth: Admin-only login (restrict to specific emails via Firebase Security Rules or a whitelist in /admins node). ◦ Assume I’m the only admin initially; include UI to view customer list. • Customer Management: ◦ Display list of registered customers (fetch from Firebase: /users, with fields: UID, name, email, FCM token). ◦ Include search/filter by name or email. • Remote Programming: ◦ Select a customer, initiate a remote session (no approval needed). ◦ Session UI: Form fields mirroring client app (e.g., card_id, user_name, access_level, expiration_date), plus inputs for SALTO-specific data (e.g., decoded programming blocks or keys). ◦ Fill fields, send JSON data to customer’s Firebase node (e.g., /remote_sessions/{customer_uid}/data), integrating with existing web app for SALTO decoding. ◦ Real-time monitor session status (e.g., “Waiting for card”, “Programming successful”). ◦ Button to end session. ◦ Send push notification to customer via FCM when session starts. • Batch Programming: ◦ Option to send pre-filled programming templates (including SALTO payloads) to one or multiple customers without a full session. ◦ Customer receives data as a notification, opens app to review and program. • UI/UX: ◦ Dashboard: Customer list, search bar, “Start Remote Session” and “Send Template” buttons. ◦ Session page: Form fields matching client app, “Send Data” button, real-time status log. ◦ Use navigation (e.g., React Router) with sections: Customers, Session History, Settings. ◦ Use Material-UI or similar for consistency with Android Material design. • Security: ◦ Restrict admin access via Firebase Security Rules (e.g., only admins can write to /remote_sessions). ◦ Log all actions in Firebase for auditing (e.g., /session_logs). Backend Setup (Firebase) • Provide setup instructions for a Firebase project: ◦ Enable Authentication (Email, Google), Realtime Database, Cloud Messaging. ◦ Database structure: ▪ /users/{uid}: {name, email, role: “customer” or “admin”, fcm_token} ▪ /remote_sessions/{customer_uid}: {admin_uid, status: “active/completed/failed”, data: {card_id, user_name, access_level, expiration_date, salto_payload: {sectors: […], keys: […]}}, timestamp} ▪ /session_logs/{session_id}: {customer_uid, admin_uid, action, timestamp} ◦ Security Rules (example rules.json): ▪ Auth required for all reads/writes. ▪ Customers can only access their own /remote_sessions/{uid}. ▪ Admins can read/write all /remote_sessions and /users. ▪ Only admins can write to /admins. • No custom server needed; use Firebase SDKs (Android for client, Web for admin). Include web config for existing SALTO decoding integration. Implementation Guidelines • Language: Kotlin for Android app; JavaScript/React for web admin. • Target: Android 14 (API 34), minimum SDK 24; Web: Modern browsers (Chrome, Firefox). • Dependencies: For Android - Firebase (auth, database, messaging), AndroidX, Material 3, Gson for JSON parsing. For Web - firebase, react, react-dom, @mui/material, react-router-dom. • Architecture: MVVM with coroutines for Android async tasks, Room for local storage (if needed). For Web: React hooks for state, Firebase listeners for real-time. • Code Structure: ◦ Client app: Modif