Create an Android application that functions as a portable hotel key encoder for Salto access control systems using NFC-enabled Android devices. The app must allow users to read, decode, and analyze data from existing Salto-programmed MIFARE Classic cards, derive the proprietary SVN protocol format including user IDs, access rights, door lists, expiration timestamps, and checksums, and then generate and write custom programming to blank compatible MIFARE Classic 1K or 4K cards. Integrate remote capabilities via a backend server for generating and pushing configurations over HTTPS, enabling users to input parameters like user details, access levels, and validity periods remotely and encode cards on-demand without physical encoders.
Start by setting up the project structure in Replit as an Android app using Kotlin, including necessary dependencies for NFC handling, networking, and cryptography. Use the Android NFC API from android.nfc and android.nfc.tech.MifareClassic for card interactions. Include libraries like OkHttp for HTTP requests, Gson for JSON parsing, and any required cryptographic utilities for handling potential encryption in Salto data.
For the frontend: Design a simple user interface with activities for scanning cards, displaying decoded data, inputting custom parameters (e.g., user ID, room/door access list as comma-separated strings, start/end timestamps in ISO format, and optional notes), and a button to encode to a blank card. Add a remote mode where the app connects to a user-specified backend URL to fetch pre-generated data payloads. Include error handling for NFC not supported, card not detected, invalid formats, and network failures. Use Material Design components for UI elements like TextViews for data display, EditTexts for inputs, Buttons for actions, and ProgressBars for operations.
For NFC reading: Implement a foreground dispatch to detect MIFARE Classic tags. Authenticate sectors using default or user-provided keys (initially assume standard MIFARE keys like FFFFFFFFFFFF, but allow input for cracked keys). Dump all sectors (0-15 for 1K cards) into a byte array, then parse the data. Focus on identifying Salto-specific patterns: Assume sector 0 for MAD (if used), sector 1 for user data, sectors 2-4 for access lists, and others for timestamps and checksums. Compare dumps from multiple sample cards (prompt user to provide hex dumps if needed during development) to reverse-engineer fields, e.g., user ID as a 4-byte integer at offset X, door list as bitmapped or array structure, expiration as Unix timestamp.
For format derivation: Include a utility class to analyze differences between card dumps. Use bitwise operations and pattern matching to map fields. For example, identify checksums via CRC16 or similar algorithms common in RFID protocols. Hardcode initial assumptions based on known MIFARE Classic layouts for access control, but make it modular for adjustments.
For custom data generation: Create a data builder class that takes user inputs and constructs a full sector-by-sector byte array matching the derived Salto format. Validate inputs (e.g., timestamps in future, access lists not exceeding card capacity). Compute and insert checksums automatically.
For NFC writing: Authenticate target blank card sectors with default keys, then write the generated byte array block by block. Handle write failures and verify by re-reading after write.
For remote integration: Set up a simple Flask or Node.js backend in a separate Replit project (link it if possible) that exposes an API endpoint like /generate-config receiving JSON with parameters and returning the encoded byte array as base64. In the Android app, use HTTPS POST to send requests and decode the response for writing. Ensure secure transmission with API keys or basic auth.
Include security considerations: Warn that this is for educational purposes only, as modifying access control systems may violate laws. Do not hardcode any proprietary keys or formats; require user input for sensitive data.
Test the app with emulated NFC if possible in Replit, or generate APK for real device testing. Provide full source code, build instructions, and a README detailing usage, required hardware (NFC Android phone, Proxmark3 for initial key cracking if needed), and how to integrate with external tools like MifareClassicTool for validation.
Output the complete project as a zipped Android Studio-compatible folder, or host it in Replit for direct forking and running.